# 自定义水果图片

游戏会优先读取 `fruit-0.jpg` 至 `fruit-10.jpg`。把自己的图片改名成这些文件名并放进本文件夹即可，不需要修改代码；例如最小等级用 `fruit-0.jpg`，最大等级用 `fruit-10.jpg`。如果某一张 JPG 尚未放入，游戏会自动显示同等级的默认 SVG，不会报错。

图片不需要手动裁圆：游戏内和“待投放水果”预览都会自动裁为圆形。建议使用正方形 JPG，主体居中，尺寸至少 400×400。
